server {
        listen 8082;
        location / {
           charset utf-8;
           root /usr/share/nginx/html;
           index index.html index.htm;
           try_files $uri /index.html;
        }
}
